'use strict';

const readFilesFn = require('../lib/fs.js');
const assert = require('assert');

describe('testing fs module', function() {
  it('should read three files and output 8 bytes of data per file', function() {
    
  });
});
